S = [x,x,x,x,x] K = some int

l = len(S)
j = 0
ans = 0 (if asking for total number) | float('inf') if ask for min | -float('inf') if ask for max

for i in range(l):
    s[i] <-- do something for s[i]
    while (a condition to make sliding window invalid)
        1. res += i-j+1 (if asking for subarray num)
        2. ans = max(ans, i-j+1)
        3. ans = min(ans, i-j+1)
        then do something for s[j]
        j++
return ans
